# 2026-09-24 — Design brief: Opname Field Desk v2.0

## What happened
User asked for a comprehensive design brief (senior product designer framing) before code, with authority to fully replace DESIGN.md.

## Decision
- **DESIGN.md v2.0 written** — full 10-section brief (principles, visual direction, tokens, 21-screen inventory, 7 flows, layouts, component library, states, responsive, a11y).
- Direction confirmed as **Opname Field Desk** (v1.0 was right; NOON glass was the detour). Rationale recorded in brief §1/§2: operate-mode audit product, glare, contrast, printable ledger mental model.
- Status palette hardened to dark-legal AA recipes (text/bg/border triplets, e.g. ACT `#166534`/`#DCFCE7`) instead of flat hexes; dark-theme variants specified.
- Two-red rule: `destructive #B42318` (actions) is separate from the DSP status red family.
- shadcn token mapping table (§3.6) added so `app.css` migration is mechanical.

## Codebase conflicts documented (to be resolved by tickets)
- `app.css` still has Benton Sans, `--color-soft-*`, scan-red, sidebar glow/gradient, `sidebar-glass`, glass toasts, NOON plum dark surfaces — all banned in v2.0.
- Roles/Permissions pages use NOON amber/violet accents → must move to semantic tokens.
- PRD §12.2 notes glassmorphism applied in components → brief supersedes; PRD polish line should flip once tokens land.

## Next
Tracer-bullet tickets drafted for review (tokens → shell → status layer → screen sweeps), then `/implement` per ticket.
